/* eslint-env node */

// eslint-disable-next-line no-magic-numbers
const ignoredMagicNumbers = [-1, 0, 1];
const indent = 2;
const maxComplexity = 50;

module.exports = {
  ignoredMagicNumbers,
  indent,
  maxComplexity,
};
